import './index.less';

const btn = document.getElementById('btnMore');
const content = document.getElementById('conMore');
if(btn && conMore) {
    btn.onclick = function() {
        content.innerHTML += '新增文字'
    }
}
